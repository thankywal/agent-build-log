# <one line: what broke, in plain words>

> Layer: `wire` | `arguments` | `loop` | `budget` | `surface` | `prompt` | `transport`

## What happened

The symptom as the person on the other end saw it. Not the stack trace, the
consequence. One or two sentences.

```
paste the real log line, error string, or token count here
```

## What I assumed

The wrong theory, honestly. Skip this and the entry teaches nothing, because
the reader arrives holding the same wrong theory you did.

## What was actually true

The measurement that settled it. Show the comparison that made it undeniable.

```
the probe, both sides of it, so the difference is visible
```

## What I changed

The fix, and why it is at that layer rather than the one above it. If you
considered a narrower fix and rejected it, say which and why.

## What generalises

One paragraph. The rule someone can carry to a different codebase.

---

Code: [`index.ts`](index.ts) · Test: [`index.test.ts`](index.test.ts)
