/**
 * <what this does, in one line>
 *
 * Standalone on purpose: no imports beyond the standard library, so it can be
 * lifted into any project. The comment above the export should name the
 * failure it exists for, not just describe the code.
 */
export function replaceMe(input: string): string {
  return input;
}
